{%- style -%}
  .mfs-section {
    padding: 100px 24px;
    background: #fff;
  }
  .mfs-inner {
    display: flex;
    align-items: center;
    max-width: 1200px;
    margin: 0 auto;
    gap: 80px;
  }
  .mfs-inner--reversed { flex-direction: row-reverse; }
  @media (max-width: 860px) {
    .mfs-inner, .mfs-inner--reversed {
      flex-direction: column;
      gap: 40px;
    }
    .mfs-section { padding: 60px 24px; }
  }

  .mfs-image-col {
    flex: 1 1 45%;
    min-height: 460px;
    border-radius: 12px;
    overflow: hidden;
    background: #EEE9E1;
  }
  .mfs-image-col img {
    width: 100%;
    height: 100%;
    object-fit: cover;
    display: block;
    min-height: 460px;
  }

  .mfs-content-col {
    flex: 1 1 50%;
  }
  .mfs-tag {
    display: inline-block;
    font-family: 'Inter', sans-serif;
    font-size: 1.05rem;
    font-weight: 600;
    letter-spacing: 0.12em;
    text-transform: uppercase;
    color: #4a7c59;
    margin-bottom: 20px;
  }
  .mfs-heading {
    font-family: 'Cormorant Garamond', serif;
    font-size: clamp(2.4rem, 3.5vw, 3.4rem);
    font-weight: 400;
    line-height: 1.2;
    color: #1A2E1A;
    margin: 0 0 24px;
    letter-spacing: -0.01em;
  }
  .mfs-heading em { font-style: italic; color: #4a7c59; }
  .mfs-text {
    font-family: 'Inter', sans-serif;
    font-size: 1.55rem;
    line-height: 1.85;
    color: #1A2E1A;
    opacity: 0.78;
    margin: 0 0 36px;
  }
  .mfs-btn {
    display: inline-block;
    background: #1A2E1A;
    color: #F8F5F0;
    font-family: 'Inter', sans-serif;
    font-size: 1.2rem;
    font-weight: 600;
    text-transform: uppercase;
    letter-spacing: 0.12em;
    padding: 16px 40px;
    border-radius: 4px;
    text-decoration: none;
    transition: background 0.2s;
  }
  .mfs-btn:hover { background: #2D4D2D; }
{%- endstyle -%}

<section class="mfs-section">
  <div class="mfs-inner {% if section.settings.image_position == 'left' %}mfs-inner--reversed{% endif %}">

    <div class="mfs-image-col">
      {%- if section.settings.image_url != blank -%}
        <img src="{{ section.settings.image_url }}"
             alt="{{ section.settings.image_alt | default: section.settings.heading }}"
             loading="lazy" width="600" height="700">
      {%- else -%}
        <div style="width:100%;height:460px;display:flex;align-items:center;justify-content:center;opacity:0.2;">
          {{ 'image' | placeholder_svg_tag: 'placeholder-svg' }}
        </div>
      {%- endif -%}
    </div>

    <div class="mfs-content-col">
      {%- if section.settings.tag != blank -%}
        <span class="mfs-tag">{{ section.settings.tag }}</span>
      {%- endif -%}
      <h2 class="mfs-heading">{{ section.settings.heading }}</h2>
      <div class="mfs-text">{{ section.settings.text }}</div>
      {%- if section.settings.button_label != blank -%}
        <a href="{{ section.settings.button_link | default: '/collections/all' }}" class="mfs-btn">{{ section.settings.button_label }}</a>
      {%- endif -%}
    </div>

  </div>
</section>

{% schema %}
{
  "name": "Feature Split",
  "tag": "section",
  "class": "section",
  "settings": [
    { "type": "select", "id": "image_position", "label": "Imagen", "options": [{ "value": "right", "label": "Derecha" }, { "value": "left", "label": "Izquierda" }], "default": "right" },
    { "type": "text", "id": "image_url", "label": "URL imagen" },
    { "type": "text", "id": "image_alt", "label": "Alt imagen" },
    { "type": "text", "id": "tag", "label": "Etiqueta pequeña" },
    { "type": "inline_richtext", "id": "heading", "label": "Título", "default": "Queremos que tu higiene dental sea libre de <em>tóxicos y plástico</em>" },
    { "type": "richtext", "id": "text", "label": "Texto", "default": "<p>Nuestro primer producto es un dentífrico en polvo 100% natural y orgánico. Su envase de aluminio (30g) se puede reutilizar infinitas veces. El producto también está disponible en bolsas de papel compostable, disminuyendo la huella de carbono y simplificando su distribución.</p><p>Además del aliento fresco, tus encías estarán sanas, tus dientes limpios, blancos y sin caries.</p><p>Si prefieres el formato crema dental, puedes elegir entre opciones con o sin flúor.</p>" },
    { "type": "text", "id": "button_label", "label": "Botón", "default": "Descubrir la tienda" },
    { "type": "url", "id": "button_link", "label": "URL botón" }
  ],
  "presets": [{ "name": "Feature Split" }]
}
{% endschema %}
